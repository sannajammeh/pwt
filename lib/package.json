{
  "name": "pwt-cli",
  "version": "0.1.0",
  "main": "./dist/cli.js",
  "type": "module",
  "files": ["dist"],
  "bin": {
    "pwt": "./dist/cli.js",
    "pwtx": "./dist/pwtx.js"
  },
  "scripts": {
    "build": "bun build ./src/cli.ts ./src/pwtx.ts --outdir ./dist --target node",
    "link": "bun run build && pnpm link --global",
    "prepublishOnly": "bun run build"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "citty": "^0.1.6",
    "consola": "^3.2.3",
    "cli-table3": "^0.6.5"
  }
}
